# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout the repository and enable LFS
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # 2. Explicitly pull the actual binary files from LFS storage
      - name: Checkout LFS objects
        run: git lfs checkout

      # 3. Install dependencies and build the Next.js project
      - name: Install and Build
        run: |
          npm ci
          npm run build

      # 4. Deploy the build to Firebase Hosting
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CUBE_8C773 }}
          channelId: live
          projectId: cube-8c773